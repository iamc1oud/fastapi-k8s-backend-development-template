# Placeholder for local/demo use only — in real clusters, provision this via
# your secrets manager (sealed-secrets, external-secrets, Vault, etc), never
# commit real credentials.
apiVersion: v1
kind: Secret
metadata:
  name: backend-db
  namespace: backend
type: Opaque
stringData:
  DB_HOST: postgres
  DB_PORT: "5432"
  DB_NAME: backend
  DB_USER: backend
  DB_PASSWORD: change-me
